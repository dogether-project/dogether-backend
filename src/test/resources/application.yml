spring:
  profiles:
    active: test

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  config: classpath:logback-test.xml
  level:
    site:
      dogether: trace
